# GENERATED by tests/routes/test_openapi_docs.py. Do not edit by hand.
# Regenerate with: UPDATE_OPENAPI_DOCS=1 pytest tests/routes/test_openapi_docs.py
openapi: 3.1.0
info:
  title: discord queue_worker/ytmusic
  version: 0.0.0
  description: 'The queue_worker/ytmusic seam, generated from the built aiohttp router by tests/routes/test_openapi_docs.py.
    Do not edit by hand. Routes only: the seam contract guarantees which routes exist, not the shape of
    their bodies -- see docs/projects/http-seam-contract.md.'
paths:
  /search/ytmusic:
    post:
      operationId: post-search-ytmusic
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
  /search/ytmusic/block:
    post:
      operationId: post-search-ytmusic-block
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
  /search/ytmusic/clear:
    post:
      operationId: post-search-ytmusic-clear
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
  /search/ytmusic/status:
    get:
      operationId: get-search-ytmusic-status
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
