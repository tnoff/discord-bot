# GENERATED by tests/routes/test_openapi_docs.py. Do not edit by hand.
# Regenerate with: UPDATE_OPENAPI_DOCS=1 pytest tests/routes/test_openapi_docs.py
openapi: 3.1.0
info:
  title: discord broker
  version: 0.0.0
  description: 'The broker seam, generated from the built aiohttp router by tests/routes/test_openapi_docs.py.
    Do not edit by hand. Routes only: the seam contract guarantees which routes exist, not the shape of
    their bodies -- see docs/projects/http-seam-contract.md.'
paths:
  /bundles:
    get:
      operationId: get-bundles
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
    post:
      operationId: post-bundles
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
  /bundles/{uuid}:
    delete:
      operationId: delete-bundles-by-uuid
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
      parameters:
      - name: uuid
        in: path
        required: true
        schema:
          type: string
  /bundles/{uuid}/finalize:
    post:
      operationId: post-bundles-by-uuid-finalize
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
      parameters:
      - name: uuid
        in: path
        required: true
        schema:
          type: string
  /cache/check:
    post:
      operationId: post-cache-check
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
  /cache/cleanup:
    post:
      operationId: post-cache-cleanup
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
  /cache/count:
    get:
      operationId: get-cache-count
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
  /downloads:
    post:
      operationId: post-downloads
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
  /downloads/register:
    post:
      operationId: post-downloads-register
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
  /prefetch:
    post:
      operationId: post-prefetch
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
  /requests/{uuid}:
    post:
      operationId: post-requests-by-uuid
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
      parameters:
      - name: uuid
        in: path
        required: true
        schema:
          type: string
  /requests/{uuid}/checkout:
    post:
      operationId: post-requests-by-uuid-checkout
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
      parameters:
      - name: uuid
        in: path
        required: true
        schema:
          type: string
  /requests/{uuid}/discard:
    post:
      operationId: post-requests-by-uuid-discard
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
      parameters:
      - name: uuid
        in: path
        required: true
        schema:
          type: string
  /requests/{uuid}/release:
    post:
      operationId: post-requests-by-uuid-release
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
      parameters:
      - name: uuid
        in: path
        required: true
        schema:
          type: string
  /requests/{uuid}/remove:
    post:
      operationId: post-requests-by-uuid-remove
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
      parameters:
      - name: uuid
        in: path
        required: true
        schema:
          type: string
  /requests/{uuid}/status:
    put:
      operationId: put-requests-by-uuid-status
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
      parameters:
      - name: uuid
        in: path
        required: true
        schema:
          type: string
  /results/next:
    get:
      operationId: get-results-next
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
  /search-results:
    post:
      operationId: post-search-results
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
  /search-results/next:
    get:
      operationId: get-search-results-next
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
  /sessions:
    get:
      operationId: get-sessions
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
  /sessions/{guild_id}:
    delete:
      operationId: delete-sessions-by-guild_id
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
      parameters:
      - name: guild_id
        in: path
        required: true
        schema:
          type: string
    put:
      operationId: put-sessions-by-guild_id
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
      parameters:
      - name: guild_id
        in: path
        required: true
        schema:
          type: string
