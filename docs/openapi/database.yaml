# GENERATED by tests/routes/test_openapi_docs.py. Do not edit by hand.
# Regenerate with: UPDATE_OPENAPI_DOCS=1 pytest tests/routes/test_openapi_docs.py
openapi: 3.1.0
info:
  title: discord database
  version: 0.0.0
  description: 'The database seam, generated from the built aiohttp router by tests/routes/test_openapi_docs.py.
    Do not edit by hand. Routes only: the seam contract guarantees which routes exist, not the shape of
    their bodies -- see docs/projects/http-seam-contract.md.'
paths:
  /database/guild_analytics/get_analytics:
    post:
      operationId: post-database-guild_analytics-get_analytics
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
  /database/guild_analytics/record_play:
    post:
      operationId: post-database-guild_analytics-record_play
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
  /database/markov/add_channel:
    post:
      operationId: post-database-markov-add_channel
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
  /database/markov/generate_words:
    post:
      operationId: post-database-markov-generate_words
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
  /database/markov/get_channel:
    post:
      operationId: post-database-markov-get_channel
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
  /database/markov/list_channels:
    post:
      operationId: post-database-markov-list_channels
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
  /database/markov/list_guild_channel_ids:
    post:
      operationId: post-database-markov-list_guild_channel_ids
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
  /database/markov/prune_relations_before:
    post:
      operationId: post-database-markov-prune_relations_before
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
  /database/markov/remove_channel:
    post:
      operationId: post-database-markov-remove_channel
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
  /database/markov/reset_channel:
    post:
      operationId: post-database-markov-reset_channel
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
  /database/markov/save_messages:
    post:
      operationId: post-database-markov-save_messages
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
  /database/playlist/add_items:
    post:
      operationId: post-database-playlist-add_items
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
  /database/playlist/count_playlists:
    post:
      operationId: post-database-playlist-count_playlists
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
  /database/playlist/create_playlist:
    post:
      operationId: post-database-playlist-create_playlist
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
  /database/playlist/delete_item:
    post:
      operationId: post-database-playlist-delete_item
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
  /database/playlist/delete_item_by_index:
    post:
      operationId: post-database-playlist-delete_item_by_index
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
  /database/playlist/delete_playlist:
    post:
      operationId: post-database-playlist-delete_playlist
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
  /database/playlist/ensure_history_playlist:
    post:
      operationId: post-database-playlist-ensure_history_playlist
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
  /database/playlist/get_history_playlist:
    post:
      operationId: post-database-playlist-get_history_playlist
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
  /database/playlist/get_playlist:
    post:
      operationId: post-database-playlist-get_playlist
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
  /database/playlist/get_playlist_by_name:
    post:
      operationId: post-database-playlist-get_playlist_by_name
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
  /database/playlist/get_playlist_size:
    post:
      operationId: post-database-playlist-get_playlist_size
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
  /database/playlist/list_items:
    post:
      operationId: post-database-playlist-list_items
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
  /database/playlist/list_playlists:
    post:
      operationId: post-database-playlist-list_playlists
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
  /database/playlist/mark_queued:
    post:
      operationId: post-database-playlist-mark_queued
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
  /database/playlist/record_history_item:
    post:
      operationId: post-database-playlist-record_history_item
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
  /database/playlist/rename_playlist:
    post:
      operationId: post-database-playlist-rename_playlist
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
  /database/video_cache/get_cache_count:
    post:
      operationId: post-database-video_cache-get_cache_count
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
  /database/video_cache/get_deletable_entries:
    post:
      operationId: post-database-video_cache-get_deletable_entries
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
  /database/video_cache/get_webpage_url_item:
    post:
      operationId: post-database-video_cache-get_webpage_url_item
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
  /database/video_cache/iterate_file:
    post:
      operationId: post-database-video_cache-iterate_file
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
  /database/video_cache/ready_remove:
    post:
      operationId: post-database-video_cache-ready_remove
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
  /database/video_cache/remove_video_cache:
    post:
      operationId: post-database-video_cache-remove_video_cache
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
