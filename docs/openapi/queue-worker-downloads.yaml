# GENERATED by tests/routes/test_openapi_docs.py. Do not edit by hand.
# Regenerate with: UPDATE_OPENAPI_DOCS=1 pytest tests/routes/test_openapi_docs.py
openapi: 3.1.0
info:
  title: discord queue_worker/downloads
  version: 0.0.0
  description: 'The queue_worker/downloads seam, generated from the built aiohttp router by tests/routes/test_openapi_docs.py.
    Do not edit by hand. Routes only: the seam contract guarantees which routes exist, not the shape of
    their bodies -- see docs/projects/http-seam-contract.md.'
paths:
  /downloads:
    post:
      operationId: post-downloads
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
  /downloads/block:
    post:
      operationId: post-downloads-block
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
  /downloads/clear:
    post:
      operationId: post-downloads-clear
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
  /downloads/status:
    get:
      operationId: get-downloads-status
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
