# GENERATED by tests/routes/test_openapi_docs.py. Do not edit by hand.
# Regenerate with: UPDATE_OPENAPI_DOCS=1 pytest tests/routes/test_openapi_docs.py
openapi: 3.1.0
info:
  title: discord dispatch
  version: 0.0.0
  description: 'The dispatch seam, generated from the built aiohttp router by tests/routes/test_openapi_docs.py.
    Do not edit by hand. Routes only: the seam contract guarantees which routes exist, not the shape of
    their bodies -- see docs/projects/http-seam-contract.md.'
paths:
  /dispatch/delete:
    post:
      operationId: post-dispatch-delete
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
  /dispatch/fetch_emojis:
    post:
      operationId: post-dispatch-fetch_emojis
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
  /dispatch/fetch_history:
    post:
      operationId: post-dispatch-fetch_history
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
  /dispatch/remove_mutable:
    post:
      operationId: post-dispatch-remove_mutable
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
  /dispatch/results/{request_id}:
    get:
      operationId: get-dispatch-results-by-request_id
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
      parameters:
      - name: request_id
        in: path
        required: true
        schema:
          type: string
  /dispatch/send:
    post:
      operationId: post-dispatch-send
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
  /dispatch/update_mutable:
    post:
      operationId: post-dispatch-update_mutable
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
  /dispatch/update_mutable_channel:
    post:
      operationId: post-dispatch-update_mutable_channel
      responses:
        default:
          description: See the handler; the seam contract covers the route, not the body.
