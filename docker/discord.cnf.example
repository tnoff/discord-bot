# Single-process bot config — for docker/docker-compose.yml.
# Entrypoint: discord-bot (cli.full). In-process dispatcher, no Redis.
#
# Secrets resolve from env vars via pyaml_env. Set them in docker/.env
# (gitignored — see .gitignore). Required:
#   DISCORD_TOKEN, SPOTIFY_CLIENT_ID, SPOTIFY_CLIENT_SECRET, YOUTUBE_API_KEY
general:
  discord_token: !ENV ${DISCORD_TOKEN}
  sql_connection_statement: postgresql://discord:discord@postgres:5432/discord
  monitoring:
    otlp:
      enabled: false
    health_server:
      enabled: true
      port: 8080
  include:
    default: true
    markov: true
    delete_messages: true
    music: true
music:
  download:
    spotify_credentials:
      client_id: !ENV ${SPOTIFY_CLIENT_ID}
      client_secret: !ENV ${SPOTIFY_CLIENT_SECRET}
    youtube_api_key: !ENV ${YOUTUBE_API_KEY}
