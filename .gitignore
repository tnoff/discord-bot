*.pyc
*.swp
__pycache__/*
secret/
logs/

# Docker Compose testing directories
data/
config/

.tox
discord_bot/.tox
discord_bot.egg-info/
.vs/
build/
docker/.env
.coverage
htmlcov/
.cache
*.db
# ...but the db pod's Dockerfile is not a sqlite file. `*.db` above matches it,
# and an ignored new file is invisible: `git status` lists nothing, the commit
# succeeds, and CI then fails on a dockerfile the build matrix points at and the
# repo does not contain.
!docker/Dockerfile.db
CLAUDE.md
claude-todos.md
.venv
.claude/
cnf/
volumes/
coverage.xml
